import { ValueHighlighterDirective } from './value-highlighter.directive';

describe('ValueHighlighterDirective', () => {
  it('should create an instance', () => {
    const directive = new ValueHighlighterDirective();
    expect(directive).toBeTruthy();
  });
});
